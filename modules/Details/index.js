import {SafeAreaView, ScrollView, Text} from 'react-native';
import React from 'react';

const Details = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Estou na tela de Details</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Details;
